server:
    port: 8001
spring:
    application:
        name: cloud-config-server
    profiles:
        active: git
    cloud:
        config:
            server:
                git:
                    uri: https://github.com/duzie/2os
                    search-paths: /**/resource
